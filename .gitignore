# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment files (contain secrets)
.env
.env.local
.env.*.local
.gstack/

# CDK synth output — ~31 MB of bundled node_modules per synth.
# Also covered by infra/.gitignore; repeated here so it is visible from the root.
infra/cdk.out/
.cdk.staging/

# Lint and debug artefacts — regenerated on demand, never reviewed
lint_report.json
lint_log.txt
lint_output.txt
clients_lint.txt
output.txt
firebase-debug.log
firestore-debug.log

# Firebase hosting cache — left over from the pre-AWS backend.
# NOTE: .firebase/hosting.ZGlzdA.cache is still tracked from that era.
.firebase/
